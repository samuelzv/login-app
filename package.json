{
  "name": "login-app",
  "version": "1.0.0",
  "description": "Create an HTML log in page that contains username and password fields. After a valid submission, the log in page should POST to the authentication endpoint listed below. Assume 200 is a successful authentication and 500 is a failed authentication.",
  "main": "src/server/server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "./node_modules/.bin/babel-node src/server/server.js"
  },
  "author": "Samuel Zuniga Vanoye <samuelzv@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "koa": "1.2.0",
    "koa-body": "1.4.0",
    "koa-router": "5.4.0",
    "koa-static": "2.0.0"
  },
  "devDependencies": {
    "babel-cli": "6.10.1",
    "babel-preset-es2015": "6.9.0"
  }
}
